server:
  port: 8081

# database 연동 설정
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/heekuutable?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: root
    password: heekuu03
  thymeleaf:
    cache: false

  # spring data jpa 설정
  jpa:
    database-platform: org.hibernate.dialect.MySQLDialect # or MySQL8Dialect
    open-in-view: false
    show-sql: true
    hibernate:
      ddl-auto: update